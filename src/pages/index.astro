---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Hero from '../components/Hero.astro';
import Footer from '../components/Footer.astro';
import PhoneMockup from '../components/PhoneMockup.astro';
---

<Layout title="DocSera - دوكسيرا">
	<Navbar />
	<main>
		<Hero />
        
        <!-- Stats Band -->
        <section class="stats-band">
            <div class="container stats-grid">
                <div class="stat-item fade-up">
                    <span class="stat-num">تحول رقمي</span>
                    <span class="stat-label">للقطاع الطبي</span>
                </div>
                <div class="stat-sep"></div>
                <div class="stat-item fade-up delay-1">
                    <span class="stat-num">انطلاقة سورية</span>
                    <span class="stat-label">بمعايير عالمية</span>
                </div>
                <div class="stat-sep"></div>
                <div class="stat-item fade-up delay-2">
                    <span class="stat-num">أمان وخصوصية</span>
                    <span class="stat-label">حماية تامة للبيانات</span>
                </div>
            </div>
        </section>

        <!-- Features Carousel -->
        <section class="features-carousel">
            <div class="carousel-track">
                <!-- Slide 1: Find Care -->
                <div class="slide-item">
                    <section class="value-sec">
                        <div class="blob-bg blob-1"></div>
                        <div class="container grid-split">
                            <div class="text-side fade-up">
                                <div class="pill-badge">سهولة الوصول</div>
                                <h2 class="text-title">اختر طبيبك بذكاء</h2>
                                <p>استعرض ملفات الأطباء، تعرف على خبراتهم ومؤهلاتهم، وشاهد موقع العيادة على الخريطة. كل المعلومات التي تحتاجها لاتخاذ القرار الصحيح.</p>
                                <ul class="feature-list">
                                    <li><span class="icon-check">✦</span> ملفات تعريفية شاملة</li>
                                    <li><span class="icon-check">✦</span> بحث مخصص حسب الاختصاص</li>
                                    <li><span class="icon-check">✦</span> خرائط جوجل مدمجة</li>
                                </ul>
                            </div>
                            <div class="visual-side fade-up delay-1">
                                <div class="mockup-scaler">
                                    <PhoneMockup src="/images/raw/map.png" alt="Map Search" />
                                </div>
                            </div>
                        </div>
                    </section>
                </div>
                
                <!-- Slide 2: Family -->
                <div class="slide-item">
                     <section class="value-sec alt-bg">
                        <div class="container grid-split reverse">
                            <div class="text-side fade-up">
                                <div class="pill-badge">رعاية عائلية</div>
                                <h2 class="text-title">حساب واحد للجميع</h2>
                                <p>لن تحتاج لتطبيقات متعددة. أضف أطفالك، والديك، وشريك حياتك إلى حسابك. أدر مواعيدهم، احفظ وصفاتهم، وتابع لقاحاتهم من مكان واحد.</p>
                                <ul class="feature-list">
                                    <li><span class="icon-check">✦</span> ملفات مستقلة لكل فرد</li>
                                    <li><span class="icon-check">✦</span> تذكير بمواعيد العائلة</li>
                                    <li><span class="icon-check">✦</span> سجل مرضى متكامل</li>
                                </ul>
                            </div>
                            <div class="visual-side fade-up delay-1">
                                <div class="mockup-scaler">
                                    <PhoneMockup src="/images/raw/family.png" alt="Family Mode" />
                                </div>
                            </div>
                        </div>
                    </section>
                </div>

                <!-- Slide 3: Chat -->
                <div class="slide-item">
                    <section class="value-sec">
                        <div class="container grid-split">
                            <div class="text-side fade-up">
                                <div class="pill-badge">تواصل مباشر</div>
                                <h2 class="text-title">تواصل مع طبيبك</h2>
                                <p>استشارات طبية فورية عبر الشات. أرسل الصور، التسجيلات الصوتية، والتقارير الطبية بكل أمان وخصوصية.</p>
                                <ul class="feature-list">
                                    <li><span class="icon-check">✦</span> محادثات مشفرة وآمنة</li>
                                    <li><span class="icon-check">✦</span> إرسال صور وتقارير</li>
                                    <li><span class="icon-check">✦</span> تسجيلات صوتية للتوضيح</li>
                                </ul>
                            </div>
                            <div class="visual-side fade-up delay-1">
                                <div class="mockup-scaler">
                                    <PhoneMockup src="/images/raw/chat.png" alt="Doctor Chat" />
                                </div>
                            </div>
                        </div>
                    </section>
                </div>

                <!-- Slide 4: Health Reports -->
                <div class="slide-item">
                     <section class="value-sec alt-bg">
                        <div class="container grid-split reverse">
                            <div class="text-side fade-up">
                                <div class="pill-badge">سجلك الصحي</div>
                                <h2 class="text-title">ملفك الطبي بيدك</h2>
                                <p>احتفظ بكل تقاريرك الطبية، وصفاتك، ونتائج التحاليل في مكان واحد. سهولة في الوصول ومشاركة فورية مع أي طبيب.</p>
                                <ul class="feature-list">
                                    <li><span class="icon-check">✦</span> أرشيف فحوصات كامل</li>
                                    <li><span class="icon-check">✦</span> وصفات طبية إلكترونية</li>
                                    <li><span class="icon-check">✦</span> متابعة اللقاحات</li>
                                </ul>
                            </div>
                            <div class="visual-side fade-up delay-1">
                                <div class="mockup-scaler">
                                    <PhoneMockup src="/images/raw/health.png" alt="Health Records" />
                                </div>
                            </div>
                        </div>
                    </section>
                </div>
            </div>
            
            <div class="carousel-dots">
                <button class="dot active" data-index="0"></button>
                <button class="dot" data-index="1"></button>
                <button class="dot" data-index="2"></button>
                <button class="dot" data-index="3"></button>
            </div>
        </section>

        <!-- Comparison Section (Glassmorphism) -->
        <section class="comparison-sec">
            <div class="container">
                <div class="comparison-grid">
                    <div class="comp-card old-way fade-up">
                        <div class="comp-header">تجربة تقليدية</div>
                        <ul class="comp-list">
                            <li>اتصالات هاتفية لا تنتهي</li>
                            <li>وصفات طبية ورقية ضائعة</li>
                            <li>مواعيد عشوائية وغير واضحة</li>
                        </ul>
                    </div>
                    <div class="comp-vs">VS</div>
                    <div class="comp-card docsera-way fade-up delay-1">
                        <div class="comp-header">تجربة DocSera</div>
                        <ul class="comp-list">
                            <li>حجز بضغطة واحدة</li>
                            <li>سجل طبي رقمي متكامل</li>
                            <li>راحة بال ووضوح تام</li>
                        </ul>
                        <div class="glass-shine"></div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Manifesto Section -->
        <section class="manifesto-sec">
            <div class="container">
                <div class="manifesto-box fade-up">
                    <h2 class="manifesto-title">نؤمن أن الرعاية الصحية في سوريا تستحق الأفضل.</h2>
                    <p class="manifesto-text">
                        نحن هنا لنقود التحول الرقمي في القطاع الطبي السوري. نجمع بين أحدث التقنيات العالمية والاحتياجات المحلية لنضع بين يديك تجربة رعاية صحية ذكية، شفافة، وسهلة الوصول للجميع.
                    </p>
                </div>
            </div>
        </section>

        <!-- FAQ Bridge -->
        <section class="faq-bridge">
            <div class="container text-center fade-up">
                <p class="bridge-text">هل لديك استفسارات أخرى؟</p>
                <a href="/faq" class="btn-link">تفضل بزيارة صفحة الأسئلة الشائعة ←</a>
            </div>
        </section>

        <!-- CTA Wave -->
        <section class="final-cta">
            <div class="wave-top">
                <svg viewBox="0 0 1440 180" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
                    <path fill="#ffffff" fill-opacity="1" d="M0,0 L1440,0 L1440,180 Q720,60 0,180 Z"></path>
                </svg>
            </div>
            
            <div class="container cta-content">
                <h2 class="text-huge" style="color:white">ابدأ رحلتك الصحية اليوم</h2>
                <p>مستقبل الطب الرقمي، يبدأ في سوريا</p>
                <div class="store-buttons">
                    <!-- App Store -->
                    <a href="#" class="official-badge">
                        <svg class="os-icon" viewBox="0 0 384 512" fill="white"><path d="M318.7 268.7c-.2-36.7 16.4-64.4 50-84.8-18.8-26.9-47.2-41.7-84.7-44.6-35.5-2.8-74.3 20.7-88.5 20.7-15 0-49.4-19.7-76.4-19.7C63.3 141.2 4 184.8 4 273.5q0 39.3 14.4 81.2c12.8 36.7 59 126.7 107.2 125.2 25.2-.6 43-17.9 75.8-17.9 31.8 0 48.3 17.9 76.4 17.9 48.6-.7 90.4-82.5 102.6-119.3-65.2-30.7-61.7-90-61.7-91.9zm-56.6-164.2c27.3-32.4 24.8-61.9 24-72.5-24.1 1.4-52 16.4-67.9 34.9-17.5 19.8-27.8 44.3-25.6 71.9 26.1 2 52.3-11.4 69.5-34.3z"/></svg>
                        <div class="badge-text">
                            <span class="small-txt">Download on the</span>
                            <span class="big-txt">App Store</span>
                        </div>
                    </a>
                    
                    <!-- Google Play -->
                    <a href="#" class="official-badge">
                        <svg class="os-icon" viewBox="0 0 512 512" fill="white"><path d="M325.3 234.3L104.6 13l280.8 161.2-60.1 60.1zM47 0C34 6.8 25.3 19.2 25.3 35.3v441.3c0 16.1 8.7 28.5 21.7 35.3l256.6-256L47 0zm425.2 225.6l-58.9-34.1-65.7 64.5 65.7 64.5 60.1-34.1c18-14.3 18-46.5-1.2-60.8zM104.6 499l280.8-161.2-60.1-60.1L104.6 499z"/></svg>
                        <div class="badge-text">
                            <span class="small-txt">GET IT ON</span>
                            <span class="big-txt">Google Play</span>
                        </div>
                    </a>
                </div>
            </div>
        </section>

	</main>
	<Footer />
</Layout>

<script>
	const observer = new IntersectionObserver((entries) => {
		entries.forEach(entry => {
			if (entry.isIntersecting) {
				entry.target.classList.add('visible');
			}
		});
	});
	document.querySelectorAll('.fade-up').forEach((el) => observer.observe(el));
</script>

<style>
    /* Stats Band */
    .stats-band {
        background: white;
        padding: 40px 0;
        border-bottom: 1px solid #F0F0F0;
    }
    .stats-grid {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 60px;
    }
    .stat-item { text-align: center; }
    .stat-num { display: block; font-size: 2rem; font-weight: 800; color: var(--c-main); }
    .stat-label { color: var(--c-text-muted); font-size: 0.9rem; }
    .stat-sep { width: 1px; height: 40px; background: #eee; }

    /* Value Sections */
    .value-sec {
        padding: 100px 0;
        position: relative;
        overflow: hidden;
    }
    .alt-bg { background: #FAFAFA; }

    /* Organic Blobs via CSS/SVG */
    .blob-bg {
        position: absolute;
        background-color: var(--c-main-light);
        border-radius: 50%;
        filter: blur(80px);
        z-index: 0;
    }
    .blob-1 {
        width: 600px; height: 600px;
        top: 0; left: -200px;
        opacity: 0.4;
    }

    .grid-split {
        display: grid;
        grid-template-columns: 1fr 1fr;
        align-items: center;
        gap: 80px;
        position: relative;
        z-index: 1;
    }
    .reverse { direction: ltr; }
    .reverse .text-side { direction: rtl; }

    .pill-badge {
        display: inline-block;
        background: rgba(0, 144, 146, 0.1);
        color: var(--c-main);
        padding: 6px 16px;
        border-radius: 20px;
        font-weight: 700;
        font-size: 0.85rem;
        margin-bottom: 20px;
    }

    .icon-check {
        color: var(--c-main);
        font-size: 1.2rem;
        line-height: 1;
        display: inline-block;
        transform: translateY(2px);
    }
    .brand-check {
        vertical-align: middle;
        margin-inline-end: 8px; /* For spacing */
    }

    .feature-list {
        list-style: none; padding: 0; margin-top: 24px;
    }
    .feature-list li {
        margin-bottom: 12px;
        font-size: 1.1rem;
        color: var(--c-text);
        font-weight: 500;
    }

    /* Carousel CSS */
    .features-carousel {
        position: relative;
        overflow: hidden;
        width: 100%;
        margin-bottom: 40px;
    }
    .carousel-track {
        display: flex;
        width: 400%;
        transition: transform 0.8s cubic-bezier(0.25, 1, 0.5, 1);
    }
    .slide-item {
        width: 25%;
        flex-shrink: 0;
    }
    .carousel-dots {
        position: absolute;
        bottom: 30px;
        left: 50%;
        transform: translateX(-50%);
        display: flex;
        gap: 10px;
        z-index: 10;
    }
    .dot {
        width: 12px; height: 12px;
        border-radius: 50%;
        background: #ddd;
        border: none;
        cursor: pointer;
        transition: all 0.3s;
    }
    .dot.active {
        background: var(--c-main);
        width: 30px;
        border-radius: 12px;
    }

    /* Steps Section */
    .steps-sec { padding: 100px 0; background: white; }
    .sec-header { margin-bottom: 60px; }
    .steps-grid {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 30px;
    }
    .step-card {
        background: white;
        padding: 30px;
        border-radius: 20px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.05);
        border: 1px solid #f5f5f5;
        text-align: center;
        width: 200px;
    }
    .step-icon { font-size: 2.5rem; margin-bottom: 16px; }
    .step-arrow { font-size: 2rem; color: #ddd; font-weight: 300; margin-top: -30px;}

    /* Final CTA */
    .final-cta {
        background: var(--c-main);
        color: white;
        /* padding-bottom: 100px; */
    }
    .cta-content { padding: 60px 0 100px; text-align: center; }
    .cta-content p { color: rgba(255,255,255,0.9); margin-bottom: 40px; font-size: 1.2rem; }
    
    .store-btn.white {
        background: white;
        color: var(--c-main);
    }
    .store-btn.white:hover { background: #f0f0f0; }

    .store-btn.white:hover { background: #f0f0f0; }

    /* MOCKUP SCALING: */
    /* Wrap usage in index.astro with this class */
    .mockup-scaler {
        position: relative;
        transform: scale(0.5);
        transform-origin: center center;
        /* Constrain flow dimensions */
        width: 310px;
        height: 672px; /* 1344 * 0.5 */
        margin: 0 auto;
        display: flex;
        justify-content: center;
    }

    @media (max-width: 900px) {
        .grid-split { grid-template-columns: 1fr; text-align: center; gap: 40px; }
        .stats-grid { flex-wrap: wrap; gap: 30px; }
        .stat-sep { display: none; }
        .steps-grid { flex-direction: column; gap: 20px; }
        .step-arrow { transform: rotate(90deg); margin: 0; }
        .reverse { direction: rtl; }
        /* Reduce scaler slightly for mobile if needed, or keep 0.5 which is 280-310px */
        .mockup-scaler {
           /* On very small screens we might want 0.45 */
           transform: scale(0.45);
           height: 600px;
        }
    }
    /* Official Store Badges */
    .store-buttons { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; }
    .official-badge {
        display: inline-flex;
        align-items: center;
        background: black;
        border: 1px solid rgba(255,255,255,0.2);
        border-radius: 8px;
        padding: 8px 16px;
        color: white;
        text-decoration: none;
        direction: ltr;
        transition: transform 0.2s, background 0.2s;
        min-width: 160px;
    }
    .official-badge:hover { transform: translateY(-3px); background: #222; }
    .os-icon { width: 28px; height: 28px; margin-right: 10px; flex-shrink: 0; }
    .badge-text { display: flex; flex-direction: column; align-items: flex-start; line-height: 1.1; }
    .small-txt { font-size: 0.65rem; text-transform: uppercase; opacity: 0.8; }
    .big-txt { font-size: 1.1rem; font-weight: 600; font-family: -apple-system, BlinkMacSystemFont, sans-serif; }

    /* Comparison Section */
    .comparison-sec { padding: 100px 0; background: #fff; position: relative; }
    .comparison-grid {
        display: grid;
        grid-template-columns: 1fr auto 1fr;
        align-items: center;
        gap: 20px;
        max-width: 1000px;
        margin: 0 auto;
    }
    .comp-card {
        padding: 50px 40px;
        border-radius: 40px;
        position: relative;
        overflow: hidden;
        transition: all 0.5s cubic-bezier(0.2, 1, 0.3, 1);
    }
    .old-way {
        background: #fbfbfb;
        color: var(--c-text-muted);
        border: 1px solid #eee;
        transform: scale(0.95);
        opacity: 0.8;
    }
    .docsera-way {
        background: rgba(255, 255, 255, 0.7);
        backdrop-filter: blur(20px) saturate(180%);
        -webkit-backdrop-filter: blur(20px) saturate(180%);
        border: 1px solid rgba(0, 144, 146, 0.2);
        color: var(--c-text);
        box-shadow: 0 30px 60px rgba(0, 144, 146, 0.1);
        z-index: 2;
    }
    .docsera-way:hover { 
        transform: scale(1.02) translateY(-5px);
        box-shadow: 0 40px 80px rgba(0, 144, 146, 0.15);
        border-color: var(--c-main);
    }
    
    .comp-header { font-size: 1.6rem; font-weight: 800; margin-bottom: 30px; letter-spacing: -0.5px; }
    .old-way .comp-header { color: #999; }
    .docsera-way .comp-header { color: var(--c-main); }
    
    .comp-list { list-style: none; padding: 0; margin: 0; }
    .comp-list li { margin-bottom: 20px; font-size: 1.15rem; font-weight: 500; position: relative; padding-right: 35px; }
    .comp-list li::before { 
        content: "✕"; 
        position: absolute; 
        right: 0; 
        color: #ccc; 
        font-weight: 400;
        font-size: 0.9rem;
    }
    .docsera-way .comp-list li::before { 
        content: "✦"; 
        color: var(--c-main); 
        font-size: 1.1rem;
    }
    
    .comp-vs {
        font-size: 2.5rem;
        font-weight: 900;
        color: #eee;
        font-style: italic;
        z-index: 1;
        transform: rotate(-5deg);
        user-select: none;
    }

    .glass-shine {
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: radial-gradient(circle, rgba(255,255,255,0.4) 0%, transparent 70%);
        opacity: 0;
        transition: opacity 0.5s;
        pointer-events: none;
    }
    .docsera-way:hover .glass-shine { opacity: 1; }

    /* Manifesto Section */
    .manifesto-sec { padding: 120px 0; background: #fafafa; border-top: 1px solid #f0f0f0; }
    .manifesto-box { max-width: 850px; margin: 0 auto; text-align: center; }
    .manifesto-title { 
        font-size: 3.5rem; 
        font-weight: 900; 
        line-height: 1.15; 
        letter-spacing: -1.5px;
        background: linear-gradient(135deg, var(--c-main), #052c2c);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        margin-bottom: 40px;
    }
    .manifesto-text { font-size: 1.4rem; color: var(--c-text); line-height: 1.8; font-weight: 400; opacity: 0.8; }

    /* FAQ Bridge */
    .faq-bridge { padding: 80px 0; background: #fff; }
    .bridge-text { font-size: 1.2rem; color: var(--c-text-muted); margin-bottom: 15px; }
    .btn-link { 
        color: var(--c-main); 
        font-weight: 700; 
        text-decoration: none; 
        font-size: 1.3rem; 
        display: inline-block;
        transition: all 0.3s;
    }
    .btn-link:hover { transform: translateX(-10px); color: #0a5f61; }

    @media (max-width: 1000px) {
        .comparison-grid { grid-template-columns: 1fr; gap: 40px; }
        .comp-vs { display: none; }
        .old-way { transform: scale(1); opacity: 1; order: 2; }
        .docsera-way { order: 1; }
        .manifesto-title { font-size: 2.5rem; }
        .manifesto-text { font-size: 1.2rem; }
    }
</style>

<script>
    const track = document.querySelector('.carousel-track');
    if (track) {
      const slides = document.querySelectorAll('.slide-item');
      const dots = document.querySelectorAll('.dot');
      let currentSlide = 0;
      
      function updateSlide(index) {
          const percent = index * 25; 
          track.style.transform = `translateX(${percent}%)`;
          dots.forEach((d, i) => d.classList.toggle('active', i === index));
          currentSlide = index;
      }
      dots.forEach((dot, index) => {
          dot.addEventListener('click', () => {
              updateSlide(index);
              resetTimer();
          });
      });
      let timer = setInterval(() => {
          updateSlide((currentSlide + 1) % slides.length);
      }, 6000);
      function resetTimer() {
          clearInterval(timer);
          timer = setInterval(() => {
              updateSlide((currentSlide + 1) % slides.length);
          }, 6000);
      }
    }
</script>
