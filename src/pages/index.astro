---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Hero from '../components/Hero.astro';
import Footer from '../components/Footer.astro';
import PhoneMockup from '../components/PhoneMockup.astro';
---

<Layout title="DocSera - دوكسيرا">
	<Navbar />
	<main>
		<Hero />
        
        <!-- Stats Band -->
        <section class="stats-band">
            <div class="container stats-grid">
                <div class="stat-item fade-up">
                    <span class="stat-num">تحول رقمي</span>
                    <span class="stat-label">للقطاع الطبي</span>
                </div>
                <div class="stat-sep"></div>
                <div class="stat-item fade-up delay-1">
                    <span class="stat-num">انطلاقة سورية</span>
                    <span class="stat-label">بمعايير عالمية</span>
                </div>
                <div class="stat-sep"></div>
                <div class="stat-item fade-up delay-2">
                    <span class="stat-num">أمان وخصوصية</span>
                    <span class="stat-label">حماية تامة للبيانات</span>
                </div>
            </div>
        </section>

        <!-- Features Carousel -->
        <section class="features-carousel">
            <div class="carousel-track">
                <!-- Slide 1: Find Care -->
                <div class="slide-item">
                    <section class="value-sec">

                        <div class="container grid-split">
                            <div class="text-side fade-up">
                                <div class="pill-badge">سهولة الوصول</div>
                                <h2 class="text-title">اختر طبيبك بذكاء</h2>
                                <p>استعرض ملفات الأطباء، تعرف على خبراتهم ومؤهلاتهم، وشاهد موقع العيادة على الخريطة. كل المعلومات التي تحتاجها لاتخاذ القرار الصحيح.</p>
                                <ul class="feature-list">
                                    <li><span class="icon-check">✦</span> ملفات تعريفية شاملة</li>
                                    <li><span class="icon-check">✦</span> بحث مخصص حسب الاختصاص</li>
                                    <li><span class="icon-check">✦</span> خرائط جوجل مدمجة</li>
                                </ul>
                            </div>
                            <div class="visual-side fade-up delay-1">
                                <div class="mockup-scaler">
                                    <PhoneMockup src="/images/raw/map.webp" alt="Map Search" />
                                </div>
                            </div>
                        </div>
                    </section>
                </div>
                
                <!-- Slide 2: Family -->
                <div class="slide-item">
                     <section class="value-sec">
                        <div class="container grid-split reverse">
                            <div class="text-side fade-up">
                                <div class="pill-badge">رعاية عائلية</div>
                                <h2 class="text-title">حساب واحد للجميع</h2>
                                <p>لن تحتاج لتطبيقات متعددة. أضف أطفالك، والديك، وشريك حياتك إلى حسابك. أدر مواعيدهم، احفظ وصفاتهم، وتابع لقاحاتهم من مكان واحد.</p>
                                <ul class="feature-list">
                                    <li><span class="icon-check">✦</span> ملفات مستقلة لكل فرد</li>
                                    <li><span class="icon-check">✦</span> تذكير بمواعيد العائلة</li>
                                    <li><span class="icon-check">✦</span> سجل مرضى متكامل</li>
                                </ul>
                            </div>
                            <div class="visual-side fade-up delay-1">
                                <div class="mockup-scaler">
                                    <PhoneMockup src="/images/raw/family.webp" alt="Family Mode" />
                                </div>
                            </div>
                        </div>
                    </section>
                </div>

                <!-- Slide 3: Chat -->
                <div class="slide-item">
                    <section class="value-sec">
                        <div class="container grid-split">
                            <div class="text-side fade-up">
                                <div class="pill-badge">تواصل مباشر</div>
                                <h2 class="text-title">تواصل مع طبيبك</h2>
                                <p>استشارات طبية فورية عبر الشات. أرسل الصور، التسجيلات الصوتية، والتقارير الطبية بكل أمان وخصوصية.</p>
                                <ul class="feature-list">
                                    <li><span class="icon-check">✦</span> محادثات مشفرة وآمنة</li>
                                    <li><span class="icon-check">✦</span> إرسال صور وتقارير</li>
                                    <li><span class="icon-check">✦</span> تسجيلات صوتية للتوضيح</li>
                                </ul>
                            </div>
                            <div class="visual-side fade-up delay-1">
                                <div class="mockup-scaler">
                                    <PhoneMockup src="/images/raw/chat.webp" alt="Doctor Chat" />
                                </div>
                            </div>
                        </div>
                    </section>
                </div>

                <!-- Slide 4: Health Reports -->
                <div class="slide-item">
                     <section class="value-sec">
                        <div class="container grid-split reverse">
                            <div class="text-side fade-up">
                                <div class="pill-badge">سجلك الصحي</div>
                                <h2 class="text-title">ملفك الطبي بيدك</h2>
                                <p>احتفظ بكل تقاريرك الطبية، وصفاتك، ونتائج التحاليل في مكان واحد. سهولة في الوصول ومشاركة فورية مع أي طبيب.</p>
                                <ul class="feature-list">
                                    <li><span class="icon-check">✦</span> أرشيف فحوصات كامل</li>
                                    <li><span class="icon-check">✦</span> وصفات طبية إلكترونية</li>
                                    <li><span class="icon-check">✦</span> متابعة اللقاحات</li>
                                </ul>
                            </div>
                            <div class="visual-side fade-up delay-1">
                                <div class="mockup-scaler">
                                    <PhoneMockup src="/images/raw/health.webp" alt="Health Records" />
                                </div>
                            </div>
                        </div>
                    </section>
                </div>
            </div>
            
            <div class="carousel-dots">
                <button class="dot active" data-index="0"></button>
                <button class="dot" data-index="1"></button>
                <button class="dot" data-index="2"></button>
                <button class="dot" data-index="3"></button>
            </div>
        </section>

        <!-- Manifesto: Editorial Typography (Moved Before Comparison) -->
        <section class="manifesto-editorial">
            <div class="container">
                <div class="manifesto-content fade-up">
                    <h2 class="huge-statement">
                        نؤمن أن<br>
                        <span class="highlight-text">الرعاية الصحية</span><br>
                        في سوريا<br>
                        تستحق الأفضل.
                    </h2>
                    <div class="manifesto-desc">
                         <p>نحن هنا لنقود التحول الرقمي. نجمع بين أحدث التقنيات العالمية والاحتياجات المحلية لنضع بين يديك تجربة طبية تليق بك وبعائلتك.</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- High-Impact Comparison: Chaos vs Clarity (Light Version) -->
        <section class="chaos-vs-clarity">
            <div class="c-container">
                <!-- The Old Way (Chaos) -->
                <div class="side-old">
                    <div class="pattern-grid"></div>
                    <div class="content-box fade-up">
                        <div class="label-badge chaos">الطريقة التقليدية</div>
                        <h3 class="chaos-title">فوضى، انتظار،<br>وتفاصيل ضائعة.</h3>
                        <div class="chaos-list">
                            <div class="c-item">
                                <svg class="c-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/><line x1="23" y1="1" x2="17" y2="7"/><line x1="17" y1="1" x2="23" y2="7"/></svg>
                                <span>خط مشغول واتصالات لا تنتهي</span>
                            </div>
                            <div class="c-item">
                                <svg class="c-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="9" y1="15" x2="15" y2="15"/></svg>
                                <span>وصفات طبية ورقية تضيع بسهولة</span>
                            </div>
                            <div class="c-item">
                                <svg class="c-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
                                <span>ساعات انتظار طويلة في العيادة</span>
                            </div>
                            <div class="c-item">
                                <svg class="c-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
                                <span>تاريخ طبي مفقود وتشخيص ناقص</span>
                            </div>
                             <div class="c-item">
                                <svg class="c-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
                                <span>مواعيد غير دقيقة وعشوائية</span>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- The DocSera Way (Clarity) -->
                <div class="side-new">
                    <div class="glass-portal fade-up delay-1">
                        <!-- <div class="glow-underlay"></div> -->
                        <div class="portal-content">
                            <div class="label-badge clarity">طريقة DocSera</div>
                            <h3 class="clarity-title">تحكم كامل،<br>ورعاية ذكية.</h3>
                            <div class="clarity-features">
                                <div class="f-item">
                                    <div class="icon-box"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg></div>
                                    <div class="f-text">
                                        <strong>سجلك الطبي بأمان</strong>
                                        <small>كل تفاصيلك الصحية محمية ومشفرة</small>
                                    </div>
                                </div>
                                <div class="f-item">
                                    <div class="icon-box"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="8" y1="14" x2="16" y2="14"/></svg></div>
                                    <div class="f-text">
                                        <strong>حجز فوري 24/7</strong>
                                        <small>اختر الوقت المناسب لك بضغطة زر</small>
                                    </div>
                                </div>
                                <div class="f-item">
                                    <div class="icon-box"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path><circle cx="9" cy="7" r="4"></circle><path d="M23 21v-2a4 4 0 0 0-3-3.87"></path><path d="M16 3.13a4 4 0 0 1 0 7.75"></path></svg></div>
                                    <div class="f-text">
                                        <strong>رعاية لكل العائلة</strong>
                                        <small>ملفات مستقلة لأطفالك وأحبائك</small>
                                    </div>
                                </div>
                                 <div class="f-item">
                                    <div class="icon-box"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"/></svg></div>
                                    <div class="f-text">
                                        <strong>تواصل مباشر</strong>
                                        <small>استشر طبيبك عبر الشات في أي وقت</small>
                                    </div>
                                </div>
                                <div class="f-item">
                                    <div class="icon-box"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg></div>
                                    <div class="f-text">
                                        <strong>تحديثات فورية</strong>
                                        <small>تنبيهات بموعدك ودورك في العيادة</small>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- DocSera Pro Section (Dark Glassmorphism) -->
        <section class="docsera-pro-section">
            <div class="container">
                <div class="pro-glass-card fade-up">
                    <div class="pro-content">
                        <div class="pro-badge">للأطباء والمختصين</div>
                        <h2 class="pro-title">هل أنت طبيب أو مختص؟</h2>
                        <p class="pro-desc">انضم إلى <strong>DocSera Pro</strong>. المنصة المتكاملة لإدارة عيادتك، تنظيم مواعيدك، والتواصل مع مرضاك بكفاءة عالية وبأدوات احترافية صممت خصيصاً لك.</p>
                    </div>
                    
                    <div class="pro-action">
                        <a href="https://pro.docsera.app" target="_blank" class="btn-pro">
                            <span>اكتشف DocSera Pro</span>
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/></svg>
                        </a>
                    </div>
                    
                    <div class="pro-visual">
                         <!-- Abstract Pro UI Elements -->
                         <div class="pro-ui-card card-1">
                            <div class="ui-header">
                                <div class="ui-dot red"></div><div class="ui-dot yellow"></div><div class="ui-dot green"></div>
                            </div>
                            <div class="ui-body">
                                <div class="ui-line w-70"></div>
                                <div class="ui-line w-50"></div>
                                <div class="ui-row">
                                    <div class="ui-box"></div>
                                    <div class="ui-box"></div>
                                </div>
                            </div>
                         </div>
                         <div class="pro-ui-card card-2">
                            <div class="ui-icon-circle">
                                 <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M16 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="8.5" cy="7" r="4"/><line x1="20" y1="8" x2="20" y2="14"/><line x1="23" y1="11" x2="17" y2="11"/></svg>
                            </div>
                            <div class="ui-line w-100"></div>
                         </div>
                         
                         <!-- App Icon -->
                         <div class="pro-app-icon">
                             <img src="/images/docsera-pro-icon.jpg" alt="DocSera Pro" />
                         </div>

                         <!-- Glow Effect -->
                         <div class="pro-glow"></div>
                    </div>
                </div>
            </div>
        </section>
        <section class="faq-bridge">
            <div class="container fade-up">
                <div class="help-card">
                    <div class="help-icon">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                           <path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"/>
                        </svg>
                    </div>
                    <div class="help-content">
                        <h3 class="help-title">هل لديك استفسارات؟</h3>
                        <p class="help-text">لقد جمعنا لك إجابات مفصلة عن كل ما يخص DocSera في مكان واحد.</p>
                    </div>
                    <div class="help-action">
                        <a href="/faq" class="btn-help">
                            تصفح الأسئلة الشائعة
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M19 12H5"/><path d="M12 19l-7-7 7-7"/></svg>
                        </a>
                    </div>
                </div>
            </div>
        </section>

        <!-- CTA Wave -->
        <section class="final-cta">
            <div class="wave-top">
                <svg viewBox="0 0 1440 180" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
                    <path fill="#ffffff" fill-opacity="1" d="M0,0 L1440,0 L1440,180 Q720,60 0,180 Z"></path>
                </svg>
            </div>
            
            <div class="container cta-content">
                <h2 class="text-huge" style="color:white">ابدأ رحلتك الصحية اليوم</h2>
                <p>مستقبل الطب الرقمي، يبدأ في سوريا</p>
                <div class="store-buttons">
                    <!-- App Store -->
                    <a href="#" class="official-badge">
                        <svg class="os-icon" viewBox="0 0 384 512" fill="white"><path d="M318.7 268.7c-.2-36.7 16.4-64.4 50-84.8-18.8-26.9-47.2-41.7-84.7-44.6-35.5-2.8-74.3 20.7-88.5 20.7-15 0-49.4-19.7-76.4-19.7C63.3 141.2 4 184.8 4 273.5q0 39.3 14.4 81.2c12.8 36.7 59 126.7 107.2 125.2 25.2-.6 43-17.9 75.8-17.9 31.8 0 48.3 17.9 76.4 17.9 48.6-.7 90.4-82.5 102.6-119.3-65.2-30.7-61.7-90-61.7-91.9zm-56.6-164.2c27.3-32.4 24.8-61.9 24-72.5-24.1 1.4-52 16.4-67.9 34.9-17.5 19.8-27.8 44.3-25.6 71.9 26.1 2 52.3-11.4 69.5-34.3z"/></svg>
                        <div class="badge-text">
                            <span class="small-txt">Download on the</span>
                            <span class="big-txt">App Store</span>
                        </div>
                    </a>
                    
                    <!-- Google Play -->
                    <a href="#" class="official-badge">
                        <svg class="os-icon" viewBox="0 0 512 512" fill="white"><path d="M325.3 234.3L104.6 13l280.8 161.2-60.1 60.1zM47 0C34 6.8 25.3 19.2 25.3 35.3v441.3c0 16.1 8.7 28.5 21.7 35.3l256.6-256L47 0zm425.2 225.6l-58.9-34.1-65.7 64.5 65.7 64.5 60.1-34.1c18-14.3 18-46.5-1.2-60.8zM104.6 499l280.8-161.2-60.1-60.1L104.6 499z"/></svg>
                        <div class="badge-text">
                            <span class="small-txt">GET IT ON</span>
                            <span class="big-txt">Google Play</span>
                        </div>
                    </a>
                </div>
            </div>
        </section>

	</main>
	<Footer />
</Layout>

<script>
	const observer = new IntersectionObserver((entries) => {
		entries.forEach(entry => {
			if (entry.isIntersecting) {
				entry.target.classList.add('visible');
			}
		});
	});
	document.querySelectorAll('.fade-up').forEach((el) => observer.observe(el));
</script>

<style>
    /* Stats Band */
    .stats-band {
        background: white;
        padding: 40px 0;
        border-bottom: 1px solid #F0F0F0;
    }
    .stats-grid {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 60px;
    }
    .stat-item { text-align: center; }
    .stat-num { display: block; font-size: 2rem; font-weight: 800; color: var(--c-main); }
    .stat-label { color: var(--c-text-muted); font-size: 0.9rem; }
    .stat-sep { width: 1px; height: 40px; background: #eee; }

    /* Value Sections */
    .value-sec {
        padding: 100px 0;
        position: relative;
        overflow: hidden;
    }
    .alt-bg { background: #FAFAFA; }

    /* Organic Blobs via CSS/SVG */
    .blob-bg {
        position: absolute;
        background-color: var(--c-main-light);
        border-radius: 50%;
        filter: blur(80px);
        z-index: 0;
    }
    .blob-1 {
        width: 600px; height: 600px;
        top: 0; left: -200px;
        opacity: 0.4;
    }

    .grid-split {
        display: grid;
        grid-template-columns: 1fr 1fr;
        align-items: center;
        gap: 80px;
        position: relative;
        z-index: 1;
    }
    .reverse { direction: ltr; }
    .reverse .text-side { direction: rtl; }

    .pill-badge {
        display: inline-block;
        background: rgba(0, 144, 146, 0.1);
        color: var(--c-main);
        padding: 6px 16px;
        border-radius: 20px;
        font-weight: 700;
        font-size: 0.85rem;
        margin-bottom: 20px;
    }

    .icon-check {
        color: var(--c-main);
        font-size: 1.2rem;
        line-height: 1;
        display: inline-block;
        transform: translateY(2px);
    }
    .brand-check {
        vertical-align: middle;
        margin-inline-end: 8px; /* For spacing */
    }

    .feature-list {
        list-style: none; padding: 0; margin-top: 24px;
    }
    .feature-list li {
        margin-bottom: 12px;
        font-size: 1.1rem;
        color: var(--c-text);
        font-weight: 500;
    }

    /* Carousel CSS */
    .features-carousel {
        position: relative;
        overflow: hidden;
        width: 100%;
        margin-bottom: 0;
    }
    .carousel-track {
        display: flex;
        width: 400%;
        transition: transform 0.8s cubic-bezier(0.25, 1, 0.5, 1);
    }
    .slide-item {
        width: 25%;
        flex-shrink: 0;
    }
    .carousel-dots {
        position: absolute;
        bottom: 30px;
        left: 50%;
        transform: translateX(-50%);
        display: flex;
        gap: 10px;
        z-index: 10;
    }
    .dot {
        width: 12px; height: 12px;
        border-radius: 50%;
        background: #ddd;
        border: none;
        cursor: pointer;
        transition: all 0.3s;
    }
    .dot.active {
        background: var(--c-main);
        width: 30px;
        border-radius: 12px;
    }

    /* Steps Section */
    .steps-sec { padding: 80px 0; background: white; }
    .sec-header { margin-bottom: 60px; }
    .steps-grid {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 30px;
    }
    .step-card {
        background: white;
        padding: 30px;
        border-radius: 20px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.05);
        border: 1px solid #f5f5f5;
        text-align: center;
        width: 200px;
    }
    .step-icon { font-size: 2.5rem; margin-bottom: 16px; }
    .step-arrow { font-size: 2rem; color: #ddd; font-weight: 300; margin-top: -30px;}

    /* Manifesto Brand Section (Full Width Immersive) */
    .manifesto-editorial {
        /* Full width background */
        background: radial-gradient(circle at top right, #009092, #004d4d);
        padding: 100px 0;
        position: relative;
        overflow: hidden;
        color: white;
        text-align: center;
        /* Clip path for dynamic top edge? Optional, keep simple for now */
    }
    
    .manifesto-content {
        max-width: 1000px;
        margin: 0 auto;
        padding: 0 40px;
        position: relative;
        z-index: 2;
    }
    
    /* Decorative Abstract Shapes (integrated into section) */
    .manifesto-editorial::before {
        content: '';
        position: absolute;
        top: -20%; right: -10%;
        width: 600px; height: 600px;
        background: rgba(255,255,255,0.03);
        border-radius: 50%;
        filter: blur(80px);
        z-index: 1;
    }
    .manifesto-editorial::after {
        content: '';
        position: absolute;
        bottom: -20%; left: -10%;
        width: 500px; height: 500px;
        background: rgba(0,255,255,0.05);
        border-radius: 50%;
        filter: blur(100px);
        z-index: 1;
    }

    .huge-statement {
        font-size: clamp(2rem, 5vw, 4.5rem);
        font-weight: 900;
        line-height: 1.2;
        margin-bottom: 40px;
        position: relative;
        z-index: 2;
    }
    
    .highlight-text {
        color: #00E0E3; /* Cyan pop */
        display: inline-block;
        position: relative;
    }
    
    .manifesto-desc {
        max-width: 700px;
        margin: 0 auto;
        font-size: 1.3rem;
        line-height: 1.8;
        color: rgba(255, 255, 255, 0.9);
        font-weight: 400;
        position: relative;
        z-index: 2;
    }
    .manifesto-desc p { border: none; padding: 0; }

    @media (max-width: 900px) {
        .manifesto-editorial { padding: 60px 20px; }
        .manifesto-content { 
            padding: 50px 24px; 
            border-radius: 30px; 
        }
        .huge-statement { font-size: 2.2rem; margin-bottom: 30px; letter-spacing: -0.5px; }
        .manifesto-desc { font-size: 1.1rem; line-height: 1.6; }
    } /* Final CTA */
    .final-cta {
        background: var(--c-main);
        color: white;
        /* padding-bottom: 100px; */
    }
    .cta-content { padding: 60px 0 100px; text-align: center; }
    .cta-content p { color: rgba(255,255,255,0.9); margin-bottom: 40px; font-size: 1.2rem; }
    
    .store-btn.white {
        background: white;
        color: var(--c-main);
    }
    .store-btn.white:hover { background: #f0f0f0; }

    .store-btn.white:hover { background: #f0f0f0; }

    /* MOCKUP SCALING: */
    /* Wrap usage in index.astro with this class */
    .mockup-scaler {
        position: relative;
        transform: scale(0.5);
        transform-origin: center center;
        /* Constrain flow dimensions */
        width: 310px;
        height: 672px; /* 1344 * 0.5 */
        margin: 0 auto;
        display: flex;
        justify-content: center;
    }

    @media (max-width: 900px) {
        .grid-split { grid-template-columns: 1fr; text-align: center; gap: 40px; }
        .stats-grid { flex-wrap: wrap; gap: 30px; }
        .stat-sep { display: none; }
        .steps-grid { flex-direction: column; gap: 20px; }
        .step-arrow { transform: rotate(90deg); margin: 0; }
        .reverse { direction: rtl; }
        /* Reduce scaler slightly for mobile if needed, or keep 0.5 which is 280-310px */
        .mockup-scaler {
           /* On very small screens we might want 0.45 */
           transform: scale(0.45);
           height: 600px;
        }
    }
    /* Official Store Badges */
    .store-buttons { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; }
    .official-badge {
        display: inline-flex;
        align-items: center;
        background: black;
        border: 1px solid rgba(255,255,255,0.2);
        border-radius: 8px;
        padding: 8px 16px;
        color: white;
        text-decoration: none;
        direction: ltr;
        transition: transform 0.2s, background 0.2s;
        min-width: 160px;
    }
    .official-badge:hover { transform: translateY(-3px); background: #222; }
    .os-icon { width: 28px; height: 28px; margin-right: 10px; flex-shrink: 0; }
    .badge-text { display: flex; flex-direction: column; align-items: flex-start; line-height: 1.1; }
    .small-txt { font-size: 0.65rem; text-transform: uppercase; opacity: 0.8; }
    .big-txt { font-size: 1.1rem; font-weight: 600; font-family: -apple-system, BlinkMacSystemFont, sans-serif; }

    /* High-Impact Comparison (Light Theme) */
    .chaos-vs-clarity {
        padding: 100px 0;
        display: flex;
        align-items: center;
        background: #FFFFFF;
        position: relative;
        overflow: hidden;
    }
    
    .c-container {
        display: grid;
        grid-template-columns: 1fr 1fr;
        width: 100%;
        max-width: 1200px;
        margin: 0 auto;
        gap: 60px;
        align-items: center;
        padding: 0 40px;
    }

    /* OLD SIDE (CHAOS) - Light/Messy */
    .side-old {
        position: relative;
        background: #F9F9F9;
        border-radius: 40px;
        padding: 60px 40px;
        border: 1px dashed #E0E0E0;
    }
    .pattern-grid {
        position: absolute;
        top: 0; left: 0; width: 100%; height: 100%;
        background-image: radial-gradient(#E0E0E0 1px, transparent 1px);
        background-size: 20px 20px;
        opacity: 0.5;
        border-radius: 40px;
    }
    
    .label-badge {
        display: inline-block;
        padding: 8px 16px;
        border-radius: 30px;
        font-size: 0.85rem;
        font-weight: 700;
        letter-spacing: 0.5px;
        margin-bottom: 24px;
        text-transform: uppercase;
    }
    .label-badge.chaos { background: #EAEAEA; color: #888; }
    
    .chaos-title {
        font-size: 2.8rem;
        color: #B0B0B0;
        line-height: 1.1;
        font-weight: 800;
        margin-bottom: 40px;
        letter-spacing: -1px;
    }
    
    .chaos-list {
        display: flex;
        flex-direction: column;
        gap: 20px;
    }
    .c-item {
        display: flex;
        align-items: center;
        gap: 16px;
        opacity: 0.7;
    }
    .c-icon {
        width: 24px;
        height: 24px;
        stroke: #999;
        flex-shrink: 0;
    }
    .c-item span {
        color: #888;
        font-size: 1.1rem;
        font-weight: 500;
        text-decoration: line-through; /* Crossed out effect for emphasis */
        text-decoration-color: rgba(0,0,0,0.1);
    }

    /* NEW SIDE (CLARITY) - Light/Glass */
    .side-new {
        position: relative;
        /* No background container, just the card */
    }
    
    .glass-portal {
        position: relative;
        width: 100%;
        background: linear-gradient(135deg, #FFFFFF, #F0FFFF);
        border: 1px solid rgba(0, 144, 146, 0.15);
        border-radius: 40px;
        padding: 60px 50px;
        box-shadow: 
            0 30px 60px -12px rgba(0, 144, 146, 0.12),
            0 18px 36px -18px rgba(0, 0, 0, 0.05);
        overflow: hidden;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .glass-portal:hover { 
        transform: translateY(-8px);
        box-shadow: 
            0 40px 70px -12px rgba(0, 144, 146, 0.2),
            0 22px 42px -18px rgba(0, 0, 0, 0.08);
    }

    .portal-content { position: relative; z-index: 2; }
    
    .label-badge.clarity { 
        background: rgba(0, 144, 146, 0.1); 
        color: var(--c-main); 
    }
    
    .clarity-title {
        font-size: 3rem;
        color: #1a1a1a;
        line-height: 1.1;
        font-weight: 900;
        margin-bottom: 50px;
        letter-spacing: -1px;
    }
    
    .clarity-features {
        display: flex;
        flex-direction: column;
        gap: 24px;
    }
    .f-item {
        display: flex;
        align-items: flex-start; /* Align top for subtitle */
        gap: 20px;
        padding-bottom: 24px;
        border-bottom: 1px solid rgba(0, 144, 146, 0.08);
    }
    .f-item:last-child { border-bottom: none; padding-bottom: 0; }
    
    .icon-box {
        width: 48px;
        height: 48px;
        background: rgba(0, 144, 146, 0.1);
        border-radius: 14px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: var(--c-main);
        flex-shrink: 0;
    }
    .icon-box svg { width: 24px; height: 24px; }
    
    .f-text { display: flex; flex-direction: column; }
    .f-text strong {
        font-size: 1.25rem;
        color: #1a1a1a;
        font-weight: 700;
        margin-bottom: 4px;
    }
    .f-text small {
        font-size: 0.95rem;
        color: #666;
        line-height: 1.4;
    }

    @media (max-width: 900px) {
        /* Slider/Value Section Mobile Spacing */
        .value-sec { padding: 40px 0; }
        
        .stats-grid { flex-direction: column; gap: 30px; }
        .stat-sep { width: 100px; height: 1px; }
        
        .grid-split { 
            grid-template-columns: 1fr; 
            text-align: center; 
            gap: 10px; /* Minimal gap */
        }
        .text-side { order: 1; }
        .visual-side { 
            order: 2; 
            margin-top: 0; 
            /* Negative margin if needed to pull phone up due to scaling whitespace */
            margin-top: -20px; 
        } 
        .reverse { direction: rtl; }
        .reverse .text-side { direction: rtl; }
        
        /* Reduce text margins to tighten vertically */
        .text-title { margin-bottom: 10px; font-size: 1.8rem; }
        .pill-badge { margin-bottom: 15px; }
        .feature-list { margin-top: 20px; }
        
        .steps-grid { flex-direction: column; }
        .step-arrow { transform: rotate(90deg); margin: 10px 0; }
        
        .c-container { grid-template-columns: 1fr; gap: 30px; padding: 0 16px; }
        .chaos-vs-clarity { padding: 40px 0; }
        
        .side-old, .side-new { 
            width: 100%; 
            margin: 0; 
            order: initial; 
            box-sizing: border-box; 
        }
        .side-old { order: 2; padding: 40px 24px; }
        .side-new { order: 1; margin-bottom: 20px; padding: 0; }

        .glass-portal { 
            width: 100%; max-width: 100%; 
            padding: 30px 20px; 
            border-radius: 24px; 
            box-sizing: border-box; 
        }
        .portal-content { width: 100%; }
        .clarity-features { width: 100%; }
        .f-item { width: 100%; }
        .clarity-title { font-size: 2rem; margin-bottom: 20px; word-wrap: break-word;}
        .chaos-title { font-size: 1.8rem; margin-bottom: 20px; }
        .f-item { gap: 12px; }
        .icon-box { width: 36px; height: 36px; min-width: 36px; }
        .icon-box svg { width: 18px; height: 18px; }
        .f-text strong { font-size: 1rem; }
        .f-text small { font-size: 0.8rem; }
        
        .mockup-scaler {
            transform: scale(0.45);
            height: 600px;
        }
    }

    /* DocSera Pro Section (Dark Theme) */
    .docsera-pro-section { padding: 80px 0; }
    
    .pro-glass-card {
        background: #0f1515; /* Very Dark Teal/Black */
        background-image: radial-gradient(circle at top right, #1a2a2a, #0f1515);
        border-radius: 30px;
        padding: 60px;
        position: relative;
        overflow: hidden;
        color: white;
        box-shadow: 0 20px 40px -10px rgba(0,0,0,0.4);
        border: 1px solid rgba(255,255,255,0.1);
        
        /* Desktop Grid Layout */
        display: grid;
        grid-template-columns: 1fr 1fr;
        grid-template-areas: 
            "content visual"
            "action visual";
        gap: 40px;
        align-items: center;
    }
    
    /* Grid Areas */
    .pro-content { grid-area: content; z-index: 2; position: relative; }
    .pro-action { grid-area: action; z-index: 2; position: relative; align-self: start; }
    .pro-visual { 
        grid-area: visual; 
        height: 100%; 
        min-height: 300px; 
        position: relative; 
        display: flex; 
        align-items: center; 
        justify-content: center;
    }
    
    .pro-badge {
        display: inline-block;
        background: rgba(0, 144, 146, 0.2);
        color: #00E0E3;
        padding: 6px 14px;
        border-radius: 20px;
        font-size: 0.85rem;
        font-weight: 700;
        margin-bottom: 20px;
        border: 1px solid rgba(0, 144, 146, 0.3);
    }
    
    .pro-title {
        font-size: 2.5rem;
        font-weight: 800;
        margin-bottom: 20px;
        line-height: 1.2;
        background: linear-gradient(to right, #fff, #bcecec);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
    
    .pro-desc {
        font-size: 1.1rem;
        color: rgba(255,255,255,0.7);
        margin: 0;
        line-height: 1.7;
        max-width: 500px;
    }
    .pro-desc strong { color: white; }
    
    .btn-pro {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        background: rgba(0, 144, 146, 0.8); /* Glassy Main Color */
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        color: white;
        padding: 16px 32px;
        border-radius: 14px;
        font-weight: 700;
        text-decoration: none;
        transition: all 0.3s ease;
        box-shadow: 0 10px 30px -5px rgba(0, 144, 146, 0.4);
    }
    .btn-pro:hover {
        background: rgba(0, 144, 146, 0.95);
        border-color: rgba(255, 255, 255, 0.4);
        box-shadow: 0 0 30px rgba(0, 224, 227, 0.4);
        transform: translateY(-2px);
    }
    .btn-pro svg { width: 20px; height: 20px; }
    
    /* Visual Cards (Abstract) */
    .pro-ui-card {
        background: rgba(255,255,255,0.05);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255,255,255,0.1);
        border-radius: 16px;
        position: absolute;
        box-shadow: 0 10px 30px rgba(0,0,0,0.3);
    }
    .card-1 {
        width: 260px; height: 180px;
        z-index: 2;
        padding: 15px;
        top: 40px; right: 40px;
        transform: rotate(-5deg);
        animation: floatPro 6s ease-in-out infinite;
    }
    .card-2 {
        width: 180px; height: 120px;
        z-index: 3;
        bottom: 30px; left: 40px;
        background: rgba(30,30,30,0.6);
        padding: 15px;
        transform: rotate(5deg);
        animation: floatPro 6s ease-in-out infinite 1s; /* delay */
    }
    
    /* UI Internal Deco */
    .ui-header { display: flex; gap: 6px; margin-bottom: 15px; }
    .ui-dot { width: 8px; height: 8px; border-radius: 50%; opacity: 0.5; }
    .ui-dot.red { background: #ff5f56; }
    .ui-dot.yellow { background: #ffbd2e; }
    .ui-dot.green { background: #27c93f; }
    .ui-body { display: flex; flex-direction: column; gap: 10px; }
    .ui-line { height: 8px; background: rgba(255,255,255,0.1); border-radius: 4px; }
    .w-70 { width: 70%; } .w-50 { width: 50%; } .w-100 { width: 100%; }
    .ui-row { display: flex; gap: 10px; margin-top: 10px; }
    .ui-box { width: 40px; height: 40px; background: rgba(255,255,255,0.05); border-radius: 8px; }
    .ui-icon-circle { 
        width: 40px; height: 40px; 
        background: rgba(0, 144, 146, 0.2); 
        border-radius: 50%; 
        display: flex; align-items: center; justify-content: center;
        color: #00E0E3;
        margin-bottom: 15px;
    }
    .ui-icon-circle svg { width: 20px; height: 20px; }

    .pro-app-icon {
        position: absolute;
        width: 70px; height: 70px;
        z-index: 10;
        bottom: 40px; right: 40px; /* Moved to Bottom Right to balance Card 2 (BL) and Card 1 (TR) */
        border-radius: 20px;
        box-shadow: 0 15px 35px rgba(0,0,0,0.5);
        animation: floatIcon 6s ease-in-out infinite;
        border: 1px solid rgba(255,255,255,0.1);
        overflow: hidden;
    }
    .pro-app-icon img { width: 100%; height: 100%; object-fit: cover; }

    .pro-glow {
        position: absolute;
        width: 300px; height: 300px;
        background: #009092;
        border-radius: 50%;
        filter: blur(120px);
        opacity: 0.15;
        top: 50%; left: 50%;
        transform: translate(-50%, -50%);
        z-index: 1;
    }
    
    @keyframes floatPro {
        0%, 100% { transform: translateY(0) rotate(-5deg); }
        50% { transform: translateY(-10px) rotate(-4deg); }
    }
    @keyframes floatIcon {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-10px); }
    }
    
    @media (max-width: 900px) {
        .pro-glass-card { 
            display: flex;
            flex-direction: column; 
            text-align: center; 
            padding: 50px 24px 40px; 
            gap: 30px; /* Space between flex items */
        }
        
        .pro-content { order: 1; }
        .pro-desc { margin: 0 auto; }
        
        /* Visual in middle, make it tall */
        .pro-visual { 
            order: 2; 
            width: 100%; 
            height: 320px; /* Taller container */
            margin: 0;
        }
        
        .pro-action { 
            order: 3; 
            width: 100%;
            display: flex;
            justify-content: center;
        }
        
        /* Adjust cards to fit in the 320px visual box */
        .card-1 { 
            top: 20px; 
            right: 50%; 
            margin-right: -100px; 
            transform: rotate(-5deg) scale(0.8);
            animation: none; /* Disable complex float on mobile for safety? Or keep distinct */
        } 
        .card-2 { 
            bottom: 10px;
            left: 50%;
            margin-left: -100px; 
            transform: rotate(5deg) scale(0.8);
        }
        
        .pro-app-icon {
            width: 60px; height: 60px; 
            bottom: 170px; 
            right: auto;
            left: 50%;
            margin-left: 90px;
        }
    }
    
    /* FAQ Bridge (Help Card) */
    .faq-bridge { padding: 60px 0 100px; background: #fff; }
    
    .help-card {
        background: radial-gradient(circle at top left, #F0FDFD 0%, #F9F9F9 100%);
        border: 1px solid rgba(0, 144, 146, 0.08);
        border-radius: 20px;
        padding: 40px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 30px;
        max-width: 900px;
        margin: 0 auto;
        box-shadow: 0 10px 40px -10px rgba(0, 144, 146, 0.05);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .help-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 50px -10px rgba(0, 144, 146, 0.1);
        border-color: rgba(0, 144, 146, 0.2);
    }
    
    .help-icon {
        color: var(--c-main);
        flex-shrink: 0;
        /* Removed circle background styles */
        display: flex; 
        align-items: center; 
        justify-content: center;
    }
    .help-icon svg { width: 48px; height: 48px; opacity: 0.8; }
    
    .help-content { flex: 1; text-align: right; }
    .help-title { font-size: 1.5rem; font-weight: 800; color: #1a1a1a; margin-bottom: 8px; }
    .help-text { font-size: 1.1rem; color: #666; font-weight: 500; margin: 0; }
    
    .btn-help {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        background: var(--c-main);
        color: white;
        padding: 14px 28px;
        border-radius: 12px;
        font-weight: 700;
        text-decoration: none;
        transition: all 0.3s ease;
        white-space: nowrap;
    }
    .btn-help:hover {
        background: #005f60;
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(0, 144, 146, 0.2);
    }
    .btn-help svg { width: 20px; height: 20px; transition: transform 0.3s; }
    .btn-help:hover svg { transform: translateX(-4px); }

    @media (max-width: 768px) {
        .help-card { flex-direction: column; text-align: center; padding: 30px 20px; gap: 20px; }
        .help-content { text-align: center; }
        .help-action { width: 100%; }
        .btn-help { 
            width: 100%; 
            justify-content: center; 
            box-sizing: border-box; 
            white-space: normal; /* Allow wrap */
            padding: 14px 20px;
        }
    }

    @media (max-width: 1000px) {
        .comparison-grid { grid-template-columns: 1fr; gap: 40px; }
        .comp-vs { display: none; }
        .old-way { transform: scale(1); opacity: 1; order: 2; }
        .docsera-way { order: 1; }
        .manifesto-title { font-size: 2.5rem; }
        .manifesto-text { font-size: 1.2rem; }
    }
</style>

<script>
    const track = document.querySelector('.carousel-track');
    if (track) {
      const slides = document.querySelectorAll('.slide-item');
      const dots = document.querySelectorAll('.dot');
      let currentSlide = 0;
      
      function updateSlide(index) {
          const percent = index * 25; 
          track.style.transform = `translateX(${percent}%)`;
          dots.forEach((d, i) => d.classList.toggle('active', i === index));
          currentSlide = index;
      }
      dots.forEach((dot, index) => {
          dot.addEventListener('click', () => {
              updateSlide(index);
              resetTimer();
          });
      });
      let timer = setInterval(() => {
          updateSlide((currentSlide + 1) % slides.length);
      }, 6000);
      function resetTimer() {
          clearInterval(timer);
          timer = setInterval(() => {
              updateSlide((currentSlide + 1) % slides.length);
          }, 6000);
      }
    }
</script>
